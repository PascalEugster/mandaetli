---
phase: 04-sharing-filtering-and-polish
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - src/app/person/page.tsx
  - src/app/organization/page.tsx
  - src/app/person/[slug]/page.tsx
  - src/app/organization/[slug]/page.tsx
  - src/app/party/[slug]/page.tsx
  - src/app/suche/page.tsx
  - src/components/profiles/PersonHeader.tsx
  - src/components/profiles/OrgHeader.tsx
  - src/components/profiles/PartyHeader.tsx
  - src/components/profiles/ConnectionList.tsx
  - src/components/profiles/VotingRecord.tsx
  - src/components/profiles/PartyMembers.tsx
  - src/components/search/SearchResults.tsx
  - src/components/lists/ActorCard.tsx
  - src/app/netzwerk/page.tsx
  - src/components/graph/ConnectionExplorer.tsx
autonomous: true
requirements:
  - DSGN-07
  - DSGN-08
  - SHAR-01

must_haves:
  truths:
    - "Profile pages are fully usable on mobile (centered header, hidden non-critical columns)"
    - "List pages show cards on mobile, tables on desktop"
    - "Graph view shows a mobile-friendly Connection Explorer below 768px"
    - "Search results are usable on mobile"
    - "All views have unique shareable URLs that restore exact state"
  artifacts:
    - path: "src/components/lists/ActorCard.tsx"
      provides: "Mobile card component for list pages"
    - path: "src/components/graph/ConnectionExplorer.tsx"
      provides: "Mobile alternative to graph (card-based actor list)"
  key_links:
    - from: "src/app/person/page.tsx"
      to: "src/components/lists/ActorCard.tsx"
      via: "Renders cards below md breakpoint"
    - from: "src/app/netzwerk/page.tsx"
      to: "src/components/graph/ConnectionExplorer.tsx"
      via: "Renders ConnectionExplorer below md breakpoint"
---

<objective>
Make the platform fully responsive on mobile and ensure all views have unique shareable URLs. List pages switch to card layout on mobile, profile headers center-align, non-critical table columns hide, graph page shows a Connection Explorer alternative on small screens.

Purpose: The platform must be usable by journalists sharing links on social media -- many readers will open on mobile phones.
Output: Responsive profile pages, mobile card layouts for lists, Connection Explorer for graph, shareable URL state on all views.
</objective>

<execution_context>
@/Users/pascaleugster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pascaleugster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04/04-CONTEXT.md
@.planning/phases/04/04-01-SUMMARY.md
@.planning/phases/04/04-02-SUMMARY.md

@src/app/person/page.tsx
@src/app/organization/page.tsx
@src/app/person/[slug]/page.tsx
@src/app/organization/[slug]/page.tsx
@src/app/party/[slug]/page.tsx
@src/app/suche/page.tsx
@src/app/netzwerk/page.tsx
@src/components/profiles/PersonHeader.tsx
@src/components/profiles/OrgHeader.tsx
@src/components/profiles/ConnectionList.tsx
@src/components/profiles/VotingRecord.tsx
@src/components/search/SearchResults.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mobile-responsive profile pages and list pages with card layout</name>
  <files>
    src/components/profiles/PersonHeader.tsx
    src/components/profiles/OrgHeader.tsx
    src/components/profiles/PartyHeader.tsx
    src/components/profiles/ConnectionList.tsx
    src/components/profiles/VotingRecord.tsx
    src/components/profiles/PartyMembers.tsx
    src/components/lists/ActorCard.tsx
    src/app/person/page.tsx
    src/app/organization/page.tsx
    src/app/person/[slug]/page.tsx
    src/app/organization/[slug]/page.tsx
    src/app/party/[slug]/page.tsx
  </files>
  <action>
**1. Profile headers -- mobile center alignment:**
- PersonHeader: on mobile (below md), center-align the header content. Use `text-center md:text-left` on the text container. Photo stays centered above text on mobile, moves to left on desktop.
- OrgHeader: same pattern -- center on mobile, left-aligned on desktop.
- PartyHeader: same pattern.

**2. Profile page layout -- stacked on mobile:**
- Person profile page: The `grid gap-6 lg:grid-cols-3` already stacks on mobile (below lg). Keep this. The MiniEgoGraph is already `hidden lg:block`. Add a text link "Im Netzwerk anzeigen" linking to `/netzwerk?selected={person.slug}` that shows on mobile (block lg:hidden).
- Organization profile: same pattern -- add "Im Netzwerk anzeigen" link on mobile.
- Party profile: layout already stacks on mobile, no graph to hide.

**3. Connection table -- responsive columns:**
- ConnectionList: hide "Rolle", "Bezahlt", and "Quelle" columns below md using `hidden md:table-cell` on those `<TableHead>` and `<TableCell>` elements.
- VotingRecord: hide "Rat" and "Thema" columns below md.
- PartyMembers: hide "Kanton" and "Rat" columns below md (if they exist).

**4. Create ActorCard component (`src/components/lists/ActorCard.tsx`):**
A reusable card component for displaying an actor in a list on mobile:
```typescript
type ActorCardProps = {
  name: string;
  href: string;
  subtitle?: string;      // party badge, industry, etc.
  meta?: string[];         // canton, council, connection count
  badge?: { label: string; color: string };  // party badge
};
```
- Card style: `rounded-lg border border-border-subtle bg-surface-1 p-4` with hover state
- Name as link, subtitle below, meta items as small badges/text
- Entire card is clickable (wrap in Link)

**5. List pages -- card layout on mobile:**
- Person list page (`src/app/person/page.tsx`): Show table on `hidden md:block`, show card list on `block md:hidden`. Cards show name, party badge, canton, connection count.
- Organization list page (`src/app/organization/page.tsx`): Same pattern -- cards show name, industry, connection count.
- Both use the ActorCard component.
  </action>
  <verify>
    npx tsc --noEmit passes. npm run build passes. Profile headers center on mobile. Table columns hide on mobile. List pages show cards on mobile, tables on desktop.
  </verify>
  <done>
    Profile headers center-aligned on mobile. Non-critical table columns hidden below md. List pages render cards on mobile, tables on desktop. MiniEgoGraph replaced with "Im Netzwerk anzeigen" link on mobile.
  </done>
</task>

<task type="auto">
  <name>Task 2: Connection Explorer mobile graph alternative and URL state completeness</name>
  <files>
    src/components/graph/ConnectionExplorer.tsx
    src/app/netzwerk/page.tsx
    src/app/suche/page.tsx
    src/components/search/SearchResults.tsx
  </files>
  <action>
**1. Create ConnectionExplorer component (`src/components/graph/ConnectionExplorer.tsx`):**
"use client" component -- a mobile-friendly alternative to the full network graph.

Props: same data as GraphContainer (actors, connections, parties).

Layout:
- Top: horizontal scroll of party filter chips (colored pills, clickable to toggle)
- Sort dropdown: "Meiste Verbindungen", "Name A-Z", "Name Z-A"
- Card list of actors (persons primarily), each card showing:
  - Name (bold), party badge (colored), canton
  - "X Verbindungen" count
  - Click navigates to profile page
- "Mehr laden" button at bottom (initially show 20, load 20 more each click)

Implementation:
- Filter actors by selected party chips (local state)
- Sort by selected sort option
- Use the ActorCard component from Task 1 for consistency
- Cards use same styling as list page cards

**2. Graph page mobile switching (`src/app/netzwerk/page.tsx`):**
- The page currently renders `<GraphContainer>` unconditionally
- Add responsive switching:
  - `<div className="hidden md:block h-full w-full">` around GraphContainer
  - `<div className="block md:hidden">` around ConnectionExplorer
- Both receive the same data props (actors, connections, parties)
- This means both components are loaded but only one is visible -- acceptable since ConnectionExplorer is lightweight

**3. Search page URL state (`src/app/suche/page.tsx`):**
- The search page already has `q` in searchParams
- Add `type` param for filtering by actor type: `searchParams: { q?: string; type?: string }`
- Pass `type` to SearchResults component
- In SearchResults, if `type` is set, only show that group (person/organization/party)
- Add clickable type tabs above results: "Alle", "Politiker:innen", "Organisationen", "Parteien"
- Each tab is a link: `/suche?q={q}&type={type}` -- uses standard Next.js searchParams, no nuqs needed
- Active tab highlighted with `border-b-2 border-swiss-red`

**4. SearchResults responsive adjustment:**
- Results already render as a vertical list (not a table), so they work on mobile
- Ensure proper text truncation on small screens with `truncate` on long names
  </action>
  <verify>
    npx tsc --noEmit passes. npm run build passes. Graph page shows ConnectionExplorer on narrow viewport. Search page has type filter tabs in URL. ConnectionExplorer shows filterable, sortable actor cards.
  </verify>
  <done>
    ConnectionExplorer renders on mobile as card-based alternative to graph. Graph page switches between graph and explorer at md breakpoint. Search has type filter tabs persisted in URL. All views have unique shareable URLs.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no errors
2. `npx tsc --noEmit` passes
3. Profile headers center on mobile viewport (< 768px)
4. Table columns (Rolle, Bezahlt, Quelle) hidden on mobile
5. Person/org list pages show card layout on mobile, table on desktop
6. Graph page shows ConnectionExplorer on mobile, full graph on desktop
7. ConnectionExplorer has party filter chips and sort dropdown
8. Search page has type filter tabs persistent in URL
9. All views produce unique shareable URLs
</verification>

<success_criteria>
- Profile pages fully usable on mobile (centered headers, hidden non-critical columns)
- List pages switch between card (mobile) and table (desktop) layouts
- Graph page provides a usable mobile alternative with Connection Explorer
- Search results filterable by type with URL persistence
- No horizontal scroll or layout overflow on any page at 375px width
</success_criteria>

<output>
After completion, create `.planning/phases/04/04-03-SUMMARY.md`
</output>
