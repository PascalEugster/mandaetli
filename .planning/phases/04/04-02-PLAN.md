---
phase: 04-sharing-filtering-and-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/source-tooltip.tsx
  - src/components/profiles/ConnectionList.tsx
  - src/components/profiles/ConflictScore.tsx
  - src/components/profiles/VotingRecord.tsx
  - src/components/graph/DetailPanel.tsx
  - src/app/person/[slug]/page.tsx
  - src/app/organization/[slug]/page.tsx
  - src/app/party/[slug]/page.tsx
autonomous: true
requirements:
  - DSGN-04
  - DSGN-05
  - DSGN-06
  - SHAR-02

must_haves:
  truths:
    - "Every numeric value shows its data source on hover via dotted underline tooltip"
    - "Confidence level badges are consistently visible on all connections across the platform"
    - "Conflict score uses neutral color only (no green/yellow/red)"
    - "Connection lists show first 10 items with expand/collapse for more"
    - "Graph URL state includes camera position"
  artifacts:
    - path: "src/components/ui/source-tooltip.tsx"
      provides: "Reusable source tooltip wrapper component"
    - path: "src/components/profiles/ConnectionList.tsx"
      provides: "Expand/collapse for connection lists"
    - path: "src/components/profiles/ConflictScore.tsx"
      provides: "Neutral color coding"
    - path: "src/components/graph/DetailPanel.tsx"
      provides: "ConfidenceBadge pills instead of text labels"
  key_links:
    - from: "src/components/ui/source-tooltip.tsx"
      to: "src/components/ui/tooltip.tsx"
      via: "Wraps shadcn Tooltip with source-specific styling"
    - from: "src/components/graph/DetailPanel.tsx"
      to: "src/components/data-freshness/ConfidenceBadge.tsx"
      via: "Import and render ConfidenceBadge pills"
    - from: "src/lib/graph/search-params.ts"
      to: "nuqs"
      via: "Camera params cx, cy, cz added"
---

<objective>
Implement progressive disclosure, source tooltips, confidence badge consistency, conflict score neutrality, and graph URL camera state.

Purpose: Polish data presentation so every number is traceable to its source, confidence levels are visible everywhere, and graph state is fully shareable via URL.
Output: SourceTooltip component, enhanced ConnectionList with expand/collapse, neutral ConflictScore, ConfidenceBadge in DetailPanel, camera params in URL.
</objective>

<execution_context>
@/Users/pascaleugster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pascaleugster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04/04-CONTEXT.md

@src/components/ui/tooltip.tsx
@src/components/data-freshness/ConfidenceBadge.tsx
@src/components/profiles/ConnectionList.tsx
@src/components/profiles/ConflictScore.tsx
@src/components/profiles/VotingRecord.tsx
@src/components/graph/DetailPanel.tsx
@src/lib/graph/search-params.ts
@src/app/person/[slug]/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: SourceTooltip component, confidence badge consistency, and conflict score neutrality</name>
  <files>
    src/components/ui/source-tooltip.tsx
    src/components/graph/DetailPanel.tsx
    src/components/profiles/ConflictScore.tsx
    src/app/person/[slug]/page.tsx
    src/app/organization/[slug]/page.tsx
    src/app/party/[slug]/page.tsx
  </files>
  <action>
**1. Create SourceTooltip component (`src/components/ui/source-tooltip.tsx`):**
"use client" component that wraps a number/value with a dotted underline and shows source info on hover.

```typescript
type SourceTooltipProps = {
  children: React.ReactNode;
  source: string;      // e.g., "parlament.ch"
  date?: string;       // e.g., "15.11.2025"
  url?: string;        // optional link to source
};
```

- Uses the existing shadcn `Tooltip` component from `@/components/ui/tooltip`
- Wrapper span has `border-b border-dotted border-text-muted cursor-help` styling
- Tooltip content: "Quelle: {source}" and optionally "Stand: {date}"
- If URL provided, show small external link icon

**2. Add SourceTooltip to profile pages:**
- On person profile: wrap connection count in PersonHeader with SourceTooltip (source: "parlament.ch", date: recent mock date)
- On organization profile: wrap connection count in OrgHeader with SourceTooltip
- On party profile: wrap member count and connection stats with SourceTooltip
- Pass source info as props from the server pages. Use "parlament.ch" as the default source for politician-related counts, and "Handelsregister" for org-related counts.

**3. Fix ConflictScore neutrality (`src/components/profiles/ConflictScore.tsx`):**
Per CONTEXT.md decision: "Conflict score: REMOVE color coding (green/yellow/red). Always use --text-primary and Swiss red bar."
- Remove the `scoreColor()` function entirely
- Replace `${scoreColor(score.score)}` with `text-text-primary` on the score number
- Add a horizontal progress bar below the score: a thin (4px) bar where the filled portion uses `bg-swiss-red` and the unfilled portion uses `bg-surface-3`. Width = `${score.score}%`.
- The bar is purely informational, not judgmental.

**4. Add ConfidenceBadge to graph DetailPanel (`src/components/graph/DetailPanel.tsx`):**
- Import `ConfidenceBadge` from `@/components/data-freshness/ConfidenceBadge`
- In `EdgeDetail`: replace the plain text confidence label with `<ConfidenceBadge level={attrs.confidence} />`
- In `NodeDetail` connections list: add a confidence dot (small colored circle) next to each connection. The edge attributes include `confidence` -- use `<span className="inline-block size-1.5 rounded-full" style={{ backgroundColor: confidenceColor(edgeAttrs.confidence) }} />` or import ConfidenceBadge.
- Keep the existing CONFIDENCE_LABELS map for reference but use ConfidenceBadge for rendering.
  </action>
  <verify>
    npx tsc --noEmit passes. npm run build passes. SourceTooltip renders on profile pages with dotted underline. ConflictScore shows no color-coded text (only text-primary + red bar). DetailPanel shows ConfidenceBadge pills.
  </verify>
  <done>
    SourceTooltip shows source info on hover with dotted underline. ConflictScore uses neutral colors only. DetailPanel uses ConfidenceBadge pills instead of plain text.
  </done>
</task>

<task type="auto">
  <name>Task 2: Progressive disclosure (expand/collapse, voting summary) and graph URL camera state</name>
  <files>
    src/components/profiles/ConnectionList.tsx
    src/components/profiles/VotingRecord.tsx
    src/lib/graph/search-params.ts
    src/components/graph/GraphEventHandler.tsx
  </files>
  <action>
**1. ConnectionList expand/collapse (`src/components/profiles/ConnectionList.tsx`):**
- Show only the first 10 connections by default
- Add an "Alle X anzeigen" / "Weniger anzeigen" toggle button below the table
- Use local React state `const [expanded, setExpanded] = useState(false)`
- When collapsed: render `sorted.slice(0, 10)`, show button with remaining count
- When expanded: render all, show "Weniger anzeigen" button
- Button style: `text-sm text-text-secondary hover:text-text-primary cursor-pointer mt-2`
- Only show the toggle if connections.length > 10

**2. VotingRecord summary bar (`src/components/profiles/VotingRecord.tsx`):**
- Add a stacked horizontal bar (8px height) above the vote table showing vote distribution
- Calculate counts: yes (green-600), no (red-600), abstain (yellow-600), absent/not_participating (surface-3)
- Bar segments: each segment width = `${(count / total) * 100}%`
- Use rounded corners on the outer bar: `rounded-full overflow-hidden flex h-2`
- Below the bar, show counts: "X Ja, Y Nein, Z Enthaltung, W Abwesend" in small text
- Also apply expand/collapse to the vote table: show first 10 votes by default, "Alle X anzeigen" toggle

**3. Graph camera state in URL (`src/lib/graph/search-params.ts`):**
- Add camera params to `graphSearchParams`:
  ```typescript
  cx: parseAsFloat,
  cy: parseAsFloat,
  cz: parseAsFloat,   // zoom ratio
  ```
- Import `parseAsFloat` from nuqs

**4. Update GraphEventHandler to sync camera state (`src/components/graph/GraphEventHandler.tsx`):**
- Read the file first to understand the current structure
- Add camera state syncing: when the camera moves, debounce 500ms then update URL params cx, cy, cz
- On initial load, if cx/cy/cz are in URL, set the camera position
- Use `sigma.getCamera()` to read position and `sigma.getCamera().setState({x, y, ratio})` to set
- IMPORTANT: Use a ref for the debounce timer to avoid re-render loops. The camera "updated" event fires frequently -- use `useRef` for the timeout ID, not state.
- Listen to sigma camera event: `sigma.getCamera().on("updated", handler)` -- clean up on unmount
  </action>
  <verify>
    npx tsc --noEmit passes. npm run build passes. ConnectionList shows first 10 with expand button. VotingRecord shows summary bar. Graph URL includes cx/cy/cz params after camera movement.
  </verify>
  <done>
    ConnectionList shows first 10 with "Alle X anzeigen" toggle. VotingRecord has stacked summary bar. Graph URL captures camera state with 500ms debounce. All builds pass.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with no errors
2. `npx tsc --noEmit` passes
3. SourceTooltip renders with dotted underline and shows source info on hover
4. ConflictScore renders score in text-primary color (no green/yellow/red) with Swiss red progress bar
5. DetailPanel edge detail shows ConfidenceBadge pill (not plain text)
6. ConnectionList collapses at 10 items with expand toggle
7. VotingRecord shows stacked summary bar with vote distribution
8. Graph URL includes cx/cy/cz camera params after panning/zooming
</verification>

<success_criteria>
- Every data number on profile pages has a dotted-underline tooltip showing its source
- Confidence badges are visually consistent across profile pages and graph detail panel
- Conflict score uses neutral colors only (text-primary + Swiss red bar)
- Progressive disclosure: connection lists and vote tables collapse at 10 items
- Graph camera state persisted in URL and restored on page load
</success_criteria>

<output>
After completion, create `.planning/phases/04/04-02-SUMMARY.md`
</output>
