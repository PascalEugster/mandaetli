---
phase: "03"
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/profiles/PersonHeader.tsx
  - src/components/profiles/OrgHeader.tsx
  - src/components/profiles/PartyHeader.tsx
  - src/components/profiles/ConnectionList.tsx
  - src/components/profiles/VotingRecord.tsx
  - src/components/profiles/ConflictScore.tsx
  - src/components/profiles/MiniEgoGraph.tsx
  - src/components/profiles/IndustryHeatmap.tsx
  - src/components/profiles/PartyMembers.tsx
  - src/components/lists/SortableHeader.tsx
autonomous: true
requirements: [PROF-01, PROF-02, PROF-03, PROF-04, PROF-05, PROF-06, PROF-07]

must_haves:
  truths:
    - "ConnectionList renders connections with confidence badges and source links"
    - "VotingRecord renders vote history in a sortable table"
    - "MiniEgoGraph renders a small Sigma.js graph showing 1-hop connections"
    - "IndustryHeatmap renders a cross-party industry grid"
  artifacts:
    - path: "src/components/profiles/ConnectionList.tsx"
      provides: "Reusable connection table with confidence badges"
      min_lines: 50
    - path: "src/components/profiles/MiniEgoGraph.tsx"
      provides: "Mini Sigma.js ego-graph component"
      min_lines: 40
    - path: "src/components/profiles/ConflictScore.tsx"
      provides: "COI score display with methodology"
      min_lines: 40
    - path: "src/components/profiles/IndustryHeatmap.tsx"
      provides: "Cross-party industry heatmap grid"
      min_lines: 40
  key_links:
    - from: "src/components/profiles/MiniEgoGraph.tsx"
      to: "@react-sigma/core"
      via: "dynamic import with ssr: false"
      pattern: "dynamic.*import.*MiniEgoGraph"
    - from: "src/components/profiles/ConnectionList.tsx"
      to: "src/lib/utils/formatters.ts"
      via: "connectionTypeLabel, confidenceLabel"
      pattern: "connectionTypeLabel|confidenceLabel"
---

<objective>
Create all reusable profile UI components: headers for each actor type, connection list, voting record table, conflict-of-interest score display, mini ego-graph, industry heatmap, party members list, and sortable header.

Purpose: These components are composed by the profile pages in Plan 03. Building them separately keeps each plan focused and allows parallel development with Plan 01.
Output: 10 client components in src/components/profiles/ and src/components/lists/.
</objective>

<execution_context>
@/Users/pascaleugster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pascaleugster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03/03-RESEARCH.md

@src/types/domain.ts
@src/lib/utils/formatters.ts
@src/lib/utils/constants.ts
@src/components/ui/table.tsx
@src/components/ui/badge.tsx
@src/components/ui/card.tsx
@src/components/graph/NetworkGraph.tsx
@src/lib/graph/graphStyles.ts
@src/lib/graph/buildGraph.ts
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profile header components and connection/vote tables</name>
  <files>
    src/components/profiles/PersonHeader.tsx
    src/components/profiles/OrgHeader.tsx
    src/components/profiles/PartyHeader.tsx
    src/components/profiles/ConnectionList.tsx
    src/components/profiles/VotingRecord.tsx
    src/components/profiles/PartyMembers.tsx
    src/components/lists/SortableHeader.tsx
  </files>
  <action>
All components are "use client" and use the existing design system (dark mode, surface layers, text colors from globals.css).

1. **SortableHeader** (`src/components/lists/SortableHeader.tsx`):
   - Props: `label: string, field: string, currentSort: string, currentDir: "asc" | "desc", onSort: (field: string) => void`
   - Renders a button with the label and a sort indicator arrow (ChevronUp/ChevronDown from lucide-react)
   - When clicked, toggles direction if same field, or sets new field with "asc"
   - Styling: text-text-secondary, hover:text-text-primary, active field gets text-text-primary

2. **PersonHeader** (`src/components/profiles/PersonHeader.tsx`):
   - Props: person (Actor), party (Actor | null), connectionCount: number, commissionNames: string[]
   - Layout: Horizontal flex on desktop (photo left, info right), stacked on mobile
   - Photo: 96x96 rounded-full with bg-surface-2 fallback (person icon from lucide if no portrait_url). Use next/image if portrait_url exists.
   - Info section: Full name (first_name + last_name, text-2xl font-bold text-text-primary), party badge (abbreviation with party color background), canton badge, council badge (NR/SR)
   - Below name: date_of_birth formatted Swiss, language, gender
   - Commission list: comma-separated commission names with text-text-secondary
   - Connection count: "N Verbindungen" with text-swiss-red accent

3. **OrgHeader** (`src/components/profiles/OrgHeader.tsx`):
   - Props: org (Actor), connectionCount: number
   - Name (text-2xl), industry badge, legal_form, headquarters, founded year, website link, UID
   - Connection count: "N verbundene Politiker:innen"

4. **PartyHeader** (`src/components/profiles/PartyHeader.tsx`):
   - Props: party (Actor), memberCount: number
   - Name (text-2xl), abbreviation, party color swatch (16x16 rounded div), ideology position (left-right scale: 0=links, 10=rechts as a simple horizontal bar)
   - Seats: "N Sitze im Nationalrat, M Sitze im Ständerat"
   - Founded year

5. **ConnectionList** (`src/components/profiles/ConnectionList.tsx`):
   - Props: connections array (each with: id, connection_type, confidence, role, is_paid, source_url, valid_from, otherActor: { name, slug, actor_type, industry })
   - Renders a table using the existing Table components from ui/table.tsx
   - Columns: Name (link to /person/[slug] or /organization/[slug]), Typ (connectionTypeLabel), Rolle (role text), Konfidenz (confidence badge with colored dot matching globals.css confidence colors), Bezahlt (is_paid: Ja/Nein/-), Quelle (source_url as external link icon)
   - The confidence badge should use the existing color scheme: verified=#22c55e, declared=#3b82f6, media_reported=#f59e0b, inferred=#64748b
   - Sortable by name, connection_type. Use local state for sorting (not URL) since this is a sub-component.
   - Link name to the other actor's profile page using slug

6. **VotingRecord** (`src/components/profiles/VotingRecord.tsx`):
   - Props: voteRecords array (each with: decision, vote: { affair_title, vote_date, council, topic_category, description })
   - Table columns: Datum (formatted Swiss date), Titel (affair_title), Rat (council: NR/SR), Thema (topic_category), Entscheid (decision with colored badge: yes=green, no=red, abstain=yellow, absent=gray, not_participating=gray)
   - Sortable by date (default desc), topic_category
   - Show total count: "N Abstimmungen"

7. **PartyMembers** (`src/components/profiles/PartyMembers.tsx`):
   - Props: members (Actor[]), connectionCounts: Record of string to number
   - Table with SortableHeader: Name (link to /person/[slug]), Kanton, Rat (NR/SR), Verbindungen (count)
   - Sortable by name, canton, council, connection count
   - Use nuqs for sort state so it persists in URL: parseAsString for sort field and direction

All components use the existing design tokens: bg-surface-1 for cards/sections, border-border-subtle for borders, text-text-primary/secondary/muted for text hierarchy. Use the Badge component from ui/badge.tsx for confidence and type labels.
  </action>
  <verify>
    npx tsc --noEmit && npx @biomejs/biome check src/components/profiles/ src/components/lists/
  </verify>
  <done>
    - PersonHeader renders name, photo, party badge, canton, council, commissions, connection count
    - ConnectionList renders sortable table with confidence badges and source links
    - VotingRecord renders sortable vote history table with decision badges
    - PartyMembers renders sortable member list with connection counts
    - All components compile and lint cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Mini ego-graph, conflict score, and industry heatmap components</name>
  <files>
    src/components/profiles/MiniEgoGraph.tsx
    src/components/profiles/ConflictScore.tsx
    src/components/profiles/IndustryHeatmap.tsx
  </files>
  <action>
1. **MiniEgoGraph** (`src/components/profiles/MiniEgoGraph.tsx`):
   - "use client" component
   - Props: centerActor (Actor), connections (Connection[]), neighbors (Actor[]), parties (Array of { id, color })
   - Builds a small Graphology graph: center node + neighbor nodes + edges
   - Uses the same node styling from graphStyles.ts (getNodeColor, getNodeSize equivalent but smaller scale: center=20px, neighbors=10px)
   - Uses SigmaContainer from @react-sigma/core with settings: { allowInvalidContainer: true, renderLabels: true, labelSize: 10, labelColor: { color: "#94a3b8" } }
   - Fixed height: 300px with rounded-lg border border-border-subtle bg-surface-0
   - Run ForceAtlas2 synchronously for a small graph (import { forceAtlas2 } from "graphology-layout-forceatlas2" -- the synchronous assign function, NOT the worker). Use ~200 iterations with gravity=5, scalingRatio=10.
   - On node click: navigate to that actor's profile page using router.push
   - Label: show name on all nodes (small graph, all labels visible)
   - This component must be imported via `next/dynamic` with `{ ssr: false }` in profile pages. Do NOT add the dynamic import here -- that's for the consumer (Plan 03).

2. **ConflictScore** (`src/components/profiles/ConflictScore.tsx`):
   - "use client" component
   - Props: score (ConflictScoreResult from conflict-score.ts), personName: string
   - Main display: Large percentage number (text-3xl font-bold), colored: 0-20% green, 20-50% yellow, 50%+ red
   - Subtitle: "X von Y Abstimmungen mit Interessenüberschneidung"
   - Expandable methodology section (use Collapsible from ui/collapsible.tsx):
     - Title: "Methodik" with ChevronDown icon
     - Content: Plain text explaining the algorithm:
       "Der Interessenkonflikt-Score berechnet den Anteil der Abstimmungen, bei denen [personName] in Bereichen abgestimmt hat, in denen wirtschaftliche Verbindungen bestehen. Es werden die Branchen der verbundenen Organisationen mit den Themenkategorien der Abstimmungen verglichen."
     - Show connected industries as badges
     - Show the TOPIC_TO_INDUSTRY mapping as a reference table
   - If overlappingVotes is not empty, show a collapsible list of overlapping votes with: affair_title, vote_date, decision badge, overlapping industries

3. **IndustryHeatmap** (`src/components/profiles/IndustryHeatmap.tsx`):
   - "use client" component
   - Props: data (IndustryPartyMatrix: { industries: string[], parties: Array of { abbreviation, color }, matrix: number[][] })
   - Renders a CSS grid table: columns = parties, rows = industries
   - Each cell: background color uses the party's color with opacity proportional to the count (0 connections = transparent, max connections = full opacity). Show the count number in the cell.
   - Row headers: Industry name (use INDUSTRY_LABELS from constants or just capitalize the industry key)
   - Column headers: Party abbreviation with colored dot
   - Hover on cell: tooltip showing "X Verbindungen zwischen [Party] und [Industry]"
   - Responsive: horizontally scrollable on mobile
   - Use a simple div-based grid, not an HTML table (better for responsive behavior)
  </action>
  <verify>
    npx tsc --noEmit && npx @biomejs/biome check src/components/profiles/
  </verify>
  <done>
    - MiniEgoGraph renders a small force-directed graph with center actor and 1-hop neighbors
    - ConflictScore displays score percentage with expandable methodology explanation
    - IndustryHeatmap renders a cross-party industry grid with color-coded cells
    - All components compile and lint cleanly
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes (all components type-check against domain types)
2. `npx @biomejs/biome check src/` passes
3. `npm run build` succeeds (no SSR issues -- MiniEgoGraph is client-only but not dynamically imported yet, which is fine since it's not used in any page yet)
4. All 10 component files exist and export their primary component
</verification>

<success_criteria>
- All profile header components render actor metadata in the dark-mode design system
- ConnectionList shows confidence badges with correct colors matching the graph legend
- VotingRecord shows decision badges with semantic colors
- MiniEgoGraph uses Sigma.js for a small interactive graph
- ConflictScore explains its methodology transparently
- IndustryHeatmap provides cross-party industry comparison
- SortableHeader enables reusable column sorting
</success_criteria>

<output>
After completion, create `.planning/phases/03/03-02-SUMMARY.md`
</output>
