---
phase: "03"
plan: 03
type: execute
wave: 2
depends_on: ["01", "02"]
files_modified:
  - src/app/person/page.tsx
  - src/app/person/[slug]/page.tsx
  - src/app/organization/page.tsx
  - src/app/organization/[slug]/page.tsx
  - src/app/party/page.tsx
  - src/app/party/[slug]/page.tsx
  - src/app/suche/page.tsx
  - src/components/search/GlobalSearch.tsx
  - src/components/search/SearchResults.tsx
  - src/components/layout/TopBar.tsx
  - src/components/layout/Sidebar.tsx
autonomous: true
requirements: [PROF-01, PROF-02, PROF-03, PROF-04, PROF-05, PROF-06, PROF-07, PROF-08, PROF-09, PROF-10, SRCH-01, SRCH-02, SRCH-03, SRCH-04, SRCH-05]

must_haves:
  truths:
    - "Visiting /person/[slug] shows a complete politician profile with connections, votes, COI score, and mini ego-graph"
    - "Visiting /organization/[slug] shows org profile with connected politicians and mini ego-graph"
    - "Visiting /party/[slug] shows party profile with members, industry stats, and heatmap"
    - "Typing in the TopBar search shows autocomplete results grouped by type"
    - "All list views (/person, /organization, /party) support sorting"
    - "Navigation sidebar links to all sections without 'Soon' badges"
  artifacts:
    - path: "src/app/person/[slug]/page.tsx"
      provides: "Politician profile page (SSR)"
      min_lines: 60
    - path: "src/app/organization/[slug]/page.tsx"
      provides: "Organization profile page (SSR)"
      min_lines: 40
    - path: "src/app/party/[slug]/page.tsx"
      provides: "Party profile page (SSR)"
      min_lines: 60
    - path: "src/components/search/GlobalSearch.tsx"
      provides: "TopBar search with autocomplete"
      min_lines: 50
    - path: "src/app/suche/page.tsx"
      provides: "Full search results page"
      min_lines: 30
  key_links:
    - from: "src/app/person/[slug]/page.tsx"
      to: "src/lib/data/persons.ts"
      via: "getPersonBySlug, getPersonConnections"
      pattern: "getPersonBySlug|getPersonConnections"
    - from: "src/components/search/GlobalSearch.tsx"
      to: "src/lib/data/search.ts"
      via: "searchActors RPC call"
      pattern: "searchActors"
    - from: "src/components/layout/TopBar.tsx"
      to: "src/components/search/GlobalSearch.tsx"
      via: "replaces search placeholder"
      pattern: "GlobalSearch"
    - from: "src/components/layout/Sidebar.tsx"
      to: "navigation enabled"
      via: "disabled flags removed"
      pattern: "disabled.*false|disabled.*removed"
---

<objective>
Compose all profile pages, list pages, global search, and navigation updates using the data layer (Plan 01) and UI components (Plan 02).

Purpose: This is the integration plan that wires everything together into working pages. After this plan, all 15 Phase 3 requirements are met.
Output: 7 page routes, 2 search components, updated TopBar and Sidebar.
</objective>

<execution_context>
@/Users/pascaleugster/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pascaleugster/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03/03-RESEARCH.md
@.planning/phases/03/03-01-SUMMARY.md
@.planning/phases/03/03-02-SUMMARY.md

@src/app/layout.tsx
@src/app/netzwerk/page.tsx
@src/components/layout/TopBar.tsx
@src/components/layout/Sidebar.tsx
@src/components/layout/AppShell.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Profile pages (person, organization, party)</name>
  <files>
    src/app/person/[slug]/page.tsx
    src/app/organization/[slug]/page.tsx
    src/app/party/[slug]/page.tsx
  </files>
  <action>
All profile pages are async Server Components. Use `next/dynamic` with `{ ssr: false }` for MiniEgoGraph.

**Important Next.js 16 pattern:** `params` is a Promise: `const { slug } = await params;`

1. **Person Profile** (`src/app/person/[slug]/page.tsx`):

   Import and use data functions from Plan 01 (src/lib/data/persons.ts, connections.ts, votes.ts) and components from Plan 02.

   Data fetching (two-phase):
   ```
   Phase 1: getPersonBySlug(slug) -> if !person, call notFound()
   Phase 2 (parallel): getPersonConnections(person.id), getPersonVoteRecords(person.id), getPersonCommissions(person.id), fetch party if party_id exists
   ```

   Also fetch actors needed for conflict score: collect all org IDs from connections, fetch those actors.

   Calculate conflict score using calculateConflictScore() from Plan 01.

   Fetch ego-graph data: the connections + neighbor actors are already available from getPersonConnections. Also fetch party list for party colors.

   Layout (single column, scrollable):
   - PersonHeader (full width)
   - Two-column grid on lg (stacked on mobile):
     - Left column (2/3): ConnectionList, VotingRecord
     - Right column (1/3): ConflictScore, MiniEgoGraph (dynamic import)
   - Use section headings: "Interessenbindungen" (for connections), "Abstimmungsverhalten" (for votes), "Interessenkonflikt-Score" (for COI), "Netzwerk" (for ego-graph)
   - Each section in a Card from ui/card.tsx with bg-surface-1

   Export metadata function for SSR:
   ```typescript
   export async function generateMetadata({ params }: { params: Promise<{ slug: string }> }) {
     const { slug } = await params;
     // fetch person, return { title: `${firstName} ${lastName} - Mandaetli.ch` }
   }
   ```

2. **Organization Profile** (`src/app/organization/[slug]/page.tsx`):

   Data: getOrgBySlug(slug), getOrgConnections(org.id), fetch party colors for ego-graph.

   Layout:
   - OrgHeader
   - Two-column grid on lg:
     - Left (2/3): ConnectionList (showing connected politicians)
     - Right (1/3): MiniEgoGraph (dynamic import)
   - Section headings: "Verbundene Politiker:innen", "Netzwerk"

   Metadata: `{ title: "${org.name} - Mandaetli.ch" }`

3. **Party Profile** (`src/app/party/[slug]/page.tsx`):

   Data: getPartyBySlug(slug), getPartyMembers(party.id), getPartyConnectionStats(party.id). For heatmap: need ALL parties' connection stats. Fetch all connections + all actors (similar to graph page) to build IndustryPartyMatrix.

   Layout:
   - PartyHeader
   - PartyMembers (full width, sortable table)
   - Two-column on lg:
     - Left (2/3): IndustryHeatmap
     - Right (1/3): Top industries list (derived from getPartyConnectionStats)
   - Section headings: "Mitglieder", "Branchenverbindungen", "Top Branchen"

   Metadata: `{ title: "${party.abbreviation} (${party.name}) - Mandaetli.ch" }`

Use the existing design system throughout: bg-base for page background, Cards with bg-surface-1, proper spacing (p-6 for sections, gap-6 between cards).
  </action>
  <verify>
    npx tsc --noEmit && npm run build
  </verify>
  <done>
    - /person/[slug] renders complete politician profile with all sections
    - /organization/[slug] renders org profile with connected politicians and ego-graph
    - /party/[slug] renders party profile with members, industry stats, and heatmap
    - All pages render server-side (SSR) with proper metadata
    - Build succeeds without errors
  </done>
</task>

<task type="auto">
  <name>Task 2: List pages, global search, and navigation activation</name>
  <files>
    src/app/person/page.tsx
    src/app/organization/page.tsx
    src/app/party/page.tsx
    src/app/suche/page.tsx
    src/components/search/GlobalSearch.tsx
    src/components/search/SearchResults.tsx
    src/components/layout/TopBar.tsx
    src/components/layout/Sidebar.tsx
  </files>
  <action>
1. **List Pages** (all are async Server Components):

   **Person List** (`src/app/person/page.tsx`):
   - Fetch all persons using listPersons() from Plan 01
   - Server Component that reads URL search params for sorting
   - Table: Name (link to /person/[slug]), Partei (abbreviation with color dot), Kanton, Rat (NR/SR), Verbindungen (count)
   - Use SortableHeader for column headers. Since this is a Server Component, implement sorting via URL params read on the server (not nuqs hooks). Parse `?sort=name&dir=asc` from searchParams.
   - Page title: "Politiker:innen" with count
   - Metadata: `{ title: "Politiker:innen - Mandaetli.ch" }`

   **Organization List** (`src/app/organization/page.tsx`):
   - Fetch all orgs using listOrganizations()
   - Table: Name (link), Branche, Rechtsform, Sitz, Verbindungen
   - Sortable, same pattern
   - Metadata: `{ title: "Organisationen - Mandaetli.ch" }`

   **Party List** (`src/app/party/page.tsx`):
   - Fetch all parties using listParties()
   - Cards instead of table (only ~8 parties): Card per party with color accent, abbreviation, name, seats, ideology bar, member count
   - Metadata: `{ title: "Parteien - Mandaetli.ch" }`

   For list pages that use SortableHeader (client component) but are themselves Server Components: pass the current sort/dir as props from the server, and have SortableHeader use `<Link>` with updated searchParams instead of client-side state. This avoids making the entire page a client component.

2. **GlobalSearch** (`src/components/search/GlobalSearch.tsx`):
   - "use client" component
   - Renders a search input with autocomplete dropdown
   - Uses searchActors() from Plan 01 (client-side Supabase call)
   - Debounce input by 250ms using a simple setTimeout/clearTimeout pattern
   - Dropdown shows results grouped by actor_type: "Politiker:innen", "Organisationen", "Parteien"
   - Each result: name, actor_type icon (User/Building2/Flag from lucide-react), and for persons: party abbreviation + canton
   - Clicking a result navigates to the profile page (/person/[slug], /organization/[slug], /party/[slug])
   - Pressing Enter with text navigates to /suche?q=[query]
   - Show "Keine Ergebnisse" if no matches
   - Keyboard navigation: ArrowUp/Down to navigate results, Enter to select, Escape to close
   - Max 8 results in dropdown (pass max_results: 8 to searchActors)
   - Position: absolute dropdown below the input, z-50, bg-surface-1 border-border-subtle rounded-md shadow-lg
   - Close dropdown on click outside (useEffect with document click listener)

3. **SearchResults** (`src/components/search/SearchResults.tsx`):
   - Server Component for /suche page
   - Props: query (from searchParams), results (fetched server-side)
   - Groups results by actor_type
   - Each group: heading with count, list of results linking to profiles
   - If no query: show "Suchbegriff eingeben"
   - If no results: "Keine Ergebnisse für '[query]'"

4. **Search Page** (`src/app/suche/page.tsx`):
   - Async Server Component
   - Reads `searchParams.q` to get query
   - If query exists, calls searchActors server-side (use the server client, create a server-side version of searchActors)
   - Renders SearchResults
   - Metadata: `{ title: "Suche - Mandaetli.ch" }`

5. **TopBar Update** (`src/components/layout/TopBar.tsx`):
   - Replace the static search placeholder div with the GlobalSearch component
   - Keep the existing layout structure (flex between logo and right controls)
   - GlobalSearch takes the place of the "Suche..." placeholder

6. **Sidebar Update** (`src/components/layout/Sidebar.tsx`):
   - Remove `disabled: true` from the /person, /organization, and /party nav items
   - These should now be clickable links, not grayed-out placeholders
   - Keep the same icons (Users, Building2, Flag)
  </action>
  <verify>
    npx tsc --noEmit && npm run build && npx @biomejs/biome check src/
  </verify>
  <done>
    - /person lists all 248 politicians in a sortable table
    - /organization lists all 522 organizations in a sortable table
    - /party shows all 8 parties as cards
    - /suche shows search results grouped by type
    - TopBar search shows instant autocomplete results
    - Sidebar navigation links to all sections (no disabled items)
    - All pages build and lint cleanly
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds (all SSR pages render)
2. `npx tsc --noEmit` passes
3. `npx @biomejs/biome check src/` passes
4. Profile pages exist at correct routes: /person/[slug], /organization/[slug], /party/[slug]
5. List pages exist: /person, /organization, /party
6. Search page exists: /suche
7. Sidebar no longer shows "Soon" badges for person/organization/party
8. TopBar has interactive search (replacing the placeholder)
</verification>

<success_criteria>
- All 15 Phase 3 requirements (PROF-01 through PROF-10, SRCH-01 through SRCH-05) are implemented
- Profile pages render server-side with full metadata
- Global search works with instant autocomplete
- Navigation connects all sections of the platform
- List views support column sorting
- No new npm dependencies
- Build, TypeScript, and Biome all pass
</success_criteria>

<output>
After completion, create `.planning/phases/03/03-03-SUMMARY.md`
</output>
